services:
  ampulife:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ampulife-app
    restart: always
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
